## Dream Loop Run — 2026-06-07

### Trigger
- Signal: `reveals` rel written in `edges[]`
- Handoffs with this signal: alone-time-2026-06-07 (run 1), alone-time-2026-06-07-run2, alone-time-2026-06-07-run3
- Consecutive count: 3/3

### Pattern Identified
- Class: **Encoder habit**
- Root cause: The encoder writes `reveals` from training memory as a semantic proxy for "exposes/demonstrates" — semantically plausible but structurally wrong. `reveals` is reserved for auto-generation by the canvas loader (it creates implicit reveal edges based on graph topology). Having it in `edges[]` causes duplicate or conflicting edges at render time. The SOP listed `reveals` in the VALID_RELS enum without a visual prohibition, making it invisible as a trap. The validator previously treated it as a warning (exit 1), which was caught and fixed each run — but never prevented at the source.

### Changes Made
- **Validator** (`validate-library-json.py`): Promoted `reveals` in `edges[]` from `warnings.append` to `errors.append`. Now exits 2 (blocking error) instead of exit 1 (warning). Any future encoding attempt with `reveals` in edges will fail the gate and cannot be committed.
- **SOP** (`sop-reference.md`):
  1. Added prohibition banner at the top of the "Rel Types" section: "⛔ `reveals` is NOT a valid rel for encoding — auto-generated by the canvas loader at runtime. Any `reveals` edge is a validator error (exit 2)."
  2. Updated the validator warning table entry to read "**Error (exit 2)**" instead of just noting auto-generation.
- **Renderer**: No change — constraint tightening only, no new type/rel added.
- **Dream Loop protocol** (`.claude/dream-loop.md`): Created. Protocol was referenced in LIFE.md §4 but the file did not exist. Written with the 7-step protocol, handoff schema, and guardrails.

### Gate Result
- All 9 library entries: **PASS** — all exit 0, no regressions
- Tests: **PASS** — 168/168
- Committed: YES (see commit below)

### Generations Update
- Schema change: **NO** — constraint tightening only. No new VALID_TYPE or VALID_REL was added. `reveals` remains in `VALID_RELS` (the loader needs it); only encoding it manually is now an error. No `GENERATIONS.md` entry needed.

### Signal Reset
- Dream Loop `reveals` signal reset to 0/3
- Next Alone-Time run: `kaaro-viewer` (watch, density:1.71, 7 nodes). Source expansion likely needed — this entry is structurally small.

### H5 Early Signal
H5 (Ontology Signal Hypothesis — "within 5 runs, a missing entity type or rel will surface") has not yet triggered, but the `reveals` pattern is a precursor: the encoder's instinct to write it suggests there IS a semantic gap — something like an "exposes/reveals" rel that is not covered by `association` or `causes`. If this instinct recurs (now that `reveals` is blocked), the Dream Loop should consider adding a new `exposes` rel (semantics: A makes a latent fact about B visible; not a causal claim, not a generic link). Current count: 0 runs with attempted `reveals` post-block.
